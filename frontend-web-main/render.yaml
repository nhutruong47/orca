services:
  - type: web
    name: orca-frontend
    env: static
    buildCommand: npm install && npm run build
    publishPath: dist
    autoDeploy: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
